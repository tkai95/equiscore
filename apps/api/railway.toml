[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install && npx turbo build --filter=@equiscore/api"

[deploy]
startCommand = "node apps/api/dist/main.js"
healthcheckPath = "/api/v1/health"
restartPolicyType = "on_failure"
