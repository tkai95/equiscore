[build]
builder = "nixpacks"
buildCommand = "npx turbo build --filter=@equiscore/api"

[deploy]
startCommand = "node apps/api/dist/main.js"
healthcheckPath = "/api/v1/health"
restartPolicyType = "on_failure"
